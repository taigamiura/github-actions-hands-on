name: CI

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  hello-world:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: taigamiura/github-actions-hands-on@main
   
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7

      - run: npm install
      - run: npm run build
      - uses: actions/upload-artifact@v7
        with:
          name: 'js-app'
          path: dist
